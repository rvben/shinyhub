Origin: deterministic dark-mode optical derivative of `assets/branding/selected/shinyhub-orbit-hub.png`.
Production request: preserve the exact Orbit Hub typography, cyan gradient, pale node cores, transparent dimensions, and all source geometry except the approved lower-right node/orbit join repair. Isolate the six connected components of the complete "Shiny" alpha mask, including the full y descender and antialias while excluding detached source-noise flecks, and set them to Soft Starlight #A8B4D4; classify the emblem's source pixels by proximity to deep navy, cyan, and pale, normalize the result to a binary mask, retain only its two connected orbital components, intersect them with the raw classifier to preserve every protected hole, and set the complete orbit paths plus all three connected node rings uniformly to Strong Line #2B3A63. Reconstruct the lower-right join with an antialiased #2B3A63 underlay confined to x495–550 / y425–490: close the lower concavity from the source alpha contour with Disk:16, then replace the remaining one-to-three-pixel crown dip with a supersampled cubic tangent so the node flows monotonically into the orbit. Composite the existing artwork over it. Keep "Hub", the central sparkle, pale cores, and every other pixel unchanged. Do not introduce lighter overlays, detached fragments, crop, shadow, glow, or background.
