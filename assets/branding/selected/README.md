# Selected ShinyHub logo direction

`shinyhub-orbit-hub.png` is the selected first-round direction: **Orbit Hub**.
`shinyhub-orbit-hub-dark.png` is its reversed lockup for dark surfaces: every
deep-navy element becomes Starlight (`#E8EEFF`) while the cyan elements remain
unchanged.
`shinyhub-orbit-hub-mark.png` and `shinyhub-orbit-hub-mark-dark.png` are the
matching emblem-only crops for avatars, favicons, and compact placements. Both
retain the complete orbit, all three nodes, and generous transparent clear space.

Each PNG retains embedded production provenance, mirrored in the adjacent prompt
file. The identity remains unchanged except for one approved geometry repair:
the lower-right node now joins the orbit continuously along both edges. The dark
lockup carries the same bounded contour repair and remains a deterministic
color-separated derivative of the selected source everywhere else.
