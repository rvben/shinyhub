shinyhub fleet plan  ·  fleet_id=eu  ·  server=http://SERVER

Apps (3)   legend: + create  ~ update  > adopt  - delete  = unchanged
  +  create  sales-explorer  new
  ~  update  ops-metrics     source LIVE -> 0f655f10
  -  delete  retired         fleet-managed, absent from manifest (prune candidate)

Plan: 1 to create, 1 to update, 0 to adopt, 1 to delete, 0 unchanged.

Next:
  shinyhub fleet apply --prune --yes -f FLEET.toml
      (apply 1 create + 1 update, delete 1 app (irreversible: removes data dir))
